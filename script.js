'use strict';
alert('I working')

const input = document.querySelectorAll('input');

input.addEvwentListener('change' (e) => {
	let p = createElement(p);
	let text = window.body.appendChild('p')
	text.innerHTML = e.target.value
});